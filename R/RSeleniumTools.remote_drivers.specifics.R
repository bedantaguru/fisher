

rst_remotedriver_specific_chrome <- function(
  proxy = FALSE, proxy_host = "localhost", proxy_port = 8888L,
  best_known_settings = TRUE,
  headless = FALSE,
  # 1.4 Download folder
  download_capture = FALSE, download_folder
){

  rd <- RSelenium::remoteDriver()

}

